import Phaser from 'phaser';

/**
 * BattleScene — Cena de combate.
 * Placeholder para o sistema de combate action RPG.
 * Será desenvolvido na Fase 1.
 */
export class BattleScene extends Phaser.Scene {
  constructor() {
    super({ key: 'BattleScene' });
  }

  create(): void {
    console.log('[BattleScene] Sistema de combate será implementado na Fase 1');
  }

  update(): void {
    // Combate action RPG será implementado aqui
  }
}
